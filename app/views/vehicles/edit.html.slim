header.flex.flex-col.lg:flex-row.items-center.justify-between.mb-6.gap-3
  h1.text-3xl= t('views.vehicles.edit.title')

  - if allowed_to?(:update?, @vehicle)
    = link_to t('views.vehicles.edit.back'), vehicles_path, class: 'btn-back'

= render 'form', vehicle: @vehicle
