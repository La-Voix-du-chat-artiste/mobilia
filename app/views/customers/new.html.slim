header.flex.flex-col.lg:flex-row.items-center.justify-between.mb-6.gap-3
  h1.text-3xl= t('views.customers.new.title')

  - if allowed_to?(:index?, Customer)
    = link_to t('views.customers.new.back'), customers_path, class: 'btn-back'

= render 'form', customer: @customer
