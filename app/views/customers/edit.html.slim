header.flex.flex-col.lg:flex-row.items-center.justify-between.mb-6.gap-3
  h1.text-3xl= t('views.customers.edit.title')

  - if allowed_to?(:show?, @customer)
    = link_to t('views.customers.edit.back'), customer_path(@customer), class: 'btn-back'

= render 'form', customer: @customer
