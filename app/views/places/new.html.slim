header.flex.flex-col.lg:flex-row.items-center.justify-between.mb-6.gap-3
  h1.text-3xl= t('views.places.new.title')

  - if allowed_to?(:index?, Place)
    = link_to t('views.places.new.back'), places_path, class: 'btn-back'

= render 'form', place: @place
