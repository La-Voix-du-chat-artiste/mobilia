header.flex.flex-col.lg:flex-row.items-center.justify-between.mb-6.gap-3
  h1.text-3xl= t('views.places.edit.title')

  - if allowed_to?(:show?, @place)
    = link_to t('views.places.edit.back'), places_path, class: 'btn-back'

= render 'form', place: @place
