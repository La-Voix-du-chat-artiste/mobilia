header.flex.flex-col.lg:flex-row.items-center.justify-between.mb-6.gap-3
  h1.text-3xl Modifier une présence / absence pour #{@transporter.full_name}

  = link_to '<< Retour au transporter', transporter_path(@transporter), class: 'btn-back'

= render 'form', absence: @absence
