header.flex.flex-col.lg:flex-row.items-center.justify-between.mb-6.gap-3
  h1.text-3xl= t('views.transporters.absences.edit.title', name: @transporter.full_name)

  = link_to t('views.transporters.absences.edit.back'), transporter_path(@transporter), class: 'btn-back'

= render 'form', absence: @absence
